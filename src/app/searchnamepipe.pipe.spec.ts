import { SearchnamepipePipe } from './searchnamepipe.pipe';

describe('SearchnamepipePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchnamepipePipe();
    expect(pipe).toBeTruthy();
  });
});
